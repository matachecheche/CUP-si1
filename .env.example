# CU-20 — Pasarela de pagos (Stripe Checkout)
# Claves de PRUEBA desde https://dashboard.stripe.com/test/apikeys
# El webhook secret se obtiene con `stripe listen` (local) o el dashboard (nube).
STRIPE_KEY=pk_test_REEMPLAZAR
STRIPE_SECRET=sk_test_REEMPLAZAR
STRIPE_WEBHOOK_SECRET=
STRIPE_CURRENCY=bob

# Asistente de consulta por voz (Whisper STT + GPT clasificador)
OPENAI_API_KEY=sk-REEMPLAZAR
OPENAI_WHISPER_MODEL=whisper-1
OPENAI_CHAT_MODEL=gpt-4o-mini
